import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  checkBox: {
    flexDirection: 'row',
    alignItems: 'center',
    margin: 15,
  },

  textOptionStyle: {
    fontWeight: 'bold',
    fontSize: 20,
  },
});

export default styles;
