import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  checkBox: {
    flexDirection: 'row',
    alignItems: 'center',
    margin: 3,
  },
  textOptionStyle: {
    fontWeight: 'bold',
    fontSize: 15,
  },
});

export default styles;
